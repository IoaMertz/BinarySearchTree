namespace LinkedList
{
    internal class Program
    {
        static void Main(string[] args)
        {
            LinkedList<string> linked1 = new LinkedList<string>();
            //dynamic sizing. each node holds reference to the next and previous so there is no need for contiguouw memory allocation
            // in contrast to lists. 

            
        }
    }
}